<div class="modal fade od-modal" id="addHealthRecord" tabindex="-1" aria-labelledby="addHealthTitle" aria-hidden="true"><div class="modal-dialog modal-dialog-centered"><div class="modal-content"><div class="modal-header"><h2 class="modal-title" id="addHealthTitle">Add Record for {{ $pet->name }}</h2><button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button></div><div class="modal-body">
<div class="od-fields mb-3"><div><label for="healthRecordType">Record Type *</label><select id="healthRecordType">@foreach($types as $type=>$config)<option value="{{ $type }}" @selected(old('record_type',old('vaccine_name')?'vaccination':'treatment')===$type)>{{ $config[0] }}</option>@endforeach</select></div></div>
<form class="od-fields" method="POST" action="{{ route('owner.health.store',$pet) }}" enctype="multipart/form-data" id="generalRecordForm">@csrf<input type="hidden" id="generalRecordType" name="record_type" value="{{ old('record_type','treatment') }}"><div><label for="recordDescription">Title / Details *</label><input id="recordDescription" name="description" required maxlength="1000" value="{{ old('description') }}" placeholder="e.g. Skin infection treatment"></div><div><label for="recordDate">Date *</label><input id="recordDate" type="date" name="record_date" value="{{ old('record_date',today()->toDateString()) }}" max="{{ today()->toDateString() }}" min="{{ $pet->date_of_birth?->toDateString() }}" required></div><div><label for="recordNotes">Notes</label><textarea id="recordNotes" name="notes" rows="3" maxlength="2000">{{ old('notes') }}</textarea></div><div><label for="recordFile">Attach File</label><input type="file" id="recordFile" name="file" accept=".pdf,.jpg,.jpeg,.png,.doc,.docx"><small class="op-muted">PDF, JPG, PNG, DOC or DOCX, up to 10MB.</small></div><button type="submit" class="op-button op-primary">Save Record</button></form>
<form class="od-fields" method="POST" action="{{ route('owner.health.storeVaccination',$pet) }}" id="vaccinationRecordForm" hidden>@csrf<div><label for="vaccineName">Vaccine Name *</label><input id="vaccineName" name="vaccine_name" required maxlength="255" value="{{ old('vaccine_name') }}"></div><div class="od-field-pair"><div><label for="vaccineDate">Vaccination Date *</label><input type="date" id="vaccineDate" name="vaccination_date" value="{{ old('vaccination_date',today()->toDateString()) }}" required max="{{ today()->toDateString() }}" min="{{ $pet->date_of_birth?->toDateString() }}"></div><div><label for="vaccineDue">Next Due Date</label><input type="date" id="vaccineDue" name="next_due_date" value="{{ old('next_due_date') }}"></div></div><div><label for="vaccineBatch">Batch Number</label><input id="vaccineBatch" name="batch_number" maxlength="100" value="{{ old('batch_number') }}"></div><div><label for="vaccineNotes">Notes</label><textarea id="vaccineNotes" name="notes" maxlength="2000" rows="3">{{ old('notes') }}</textarea></div><button type="submit" class="op-button op-primary">Save Vaccination</button></form>
</div></div></div></div>

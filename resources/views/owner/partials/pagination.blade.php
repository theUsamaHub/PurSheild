@if($items->hasPages())<div class="op-pagination"><span>Showing {{ $items->firstItem() }}–{{ $items->lastItem() }} of {{ $items->total() }}</span><nav aria-label="Pagination">@if($items->previousPageUrl())<a class="op-button op-small" href="{{ $items->previousPageUrl() }}" rel="prev">‹ Previous</a>@endif<span>{{ $items->currentPage() }} / {{ $items->lastPage() }}</span>@if($items->nextPageUrl())<a class="op-button op-small" href="{{ $items->nextPageUrl() }}" rel="next">Next ›</a>@endif</nav></div>@endif